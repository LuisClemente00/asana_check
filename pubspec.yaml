name: asana_check
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  permission_handler: ^11.3.0
  google_mlkit_pose_detection: ^0.10.0
  flutter_tts: ^4.1.0
  confetti: ^0.7.0
  shared_preferences: ^2.3.5
  audioplayers: ^6.1.0
  keep_screen_on: ^3.0.0
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/bali_ambient.mp3